apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

components:
  - ../../../_refs

resources:
  - kubeadmcontrolplane.yaml

patches:
  - target:
      kind: Cluster
    path: patches/cluster.yaml
