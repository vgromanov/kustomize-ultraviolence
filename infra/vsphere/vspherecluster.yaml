---
apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
kind: VSphereCluster
metadata:
  name: vsphere
spec:
  identityRef:
    kind: Secret
    name: vsphere
  server: '${vcServer}'
  thumbprint: '${vcThumbprint}'